{
  "name": "ethio-football-tele-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "digest": "bun run src/main.ts",
    "dry": "bun run src/main.ts --dry-run",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "cheerio": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
